#include "nc_brain_agent/nc_brain_agent.h"

#include <core_agent/core/navicore.h>
#include <nc_brain_agent/action/{{ header_file }}.h>

using namespace {{namespace}};

{
    {
        class_name
    }
}
::{{class_name}}(){}

{
    {
        class_name
    }
}
::~{{class_name}}()
{
}

std::string
{
    {
        class_name
    }
}
::implName()
{
    return "{{ class_name }}";
}

void
{
    {
        class_name
    }
}
implonAction(std::string source, Poco::JSON::Object::Ptr obj)
{
    std::string action = obj->get("action").convert<std::string>();

    sendResponseSuccess(source, obj->get("uuid").extract<std::string>());
}
