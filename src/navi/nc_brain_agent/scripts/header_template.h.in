#ifndef{{header_guard } }
    #define{{header_guard } }

    #include <core_agent/action/action_base.h>

namespace {
{ namespace }
}  // namespace
{
    class {
        {
            class_name
        }
    } : public ActionBase
    {
    public:
        {{class_name}}();
        virtual ~{{class_name}}();

    protected:
        virtual void implonAction(std::string source, Poco::JSON::Object::Ptr obj) override;
        std::string implName() override;
    };

    REGISTER_ACTION("action_name", {{class_name}}, ActionType::DEFAULT)
}  // namespace {{ namespace }}
#endif
